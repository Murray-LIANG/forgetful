# Pip Download and Install Pandas

# Pip Download and Install

```console
$ # Download
$ pip download --platform win_amd64 --only-binary=:all: --python-version 37 --implementation cp --abi cp37m pandas

$ # Install
$ pip install --no-index --find-links=. pandas

```
